# Cycles directory

This directory is managed by the CLI.

Each development cycle should get a subdirectory such as:

```text
.ai-loop/cycles/0001/
.ai-loop/cycles/0002/
```

Cycle artifacts are historical evidence, not foundational specifications.
