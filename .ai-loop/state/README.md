# State directory

This directory is managed by the CLI.

It may contain runtime state files such as:

```text
loop_state.json
current_cycle.json
```

Users should not normally edit these files manually.
